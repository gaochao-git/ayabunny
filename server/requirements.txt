# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
sse-starlette>=2.1.0

# LangChain
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langgraph>=0.2.0

# HTTP client
httpx>=0.27.0
aiohttp>=3.9.0
aiofiles>=24.1.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# VAD (支持多后端: ten, webrtc, silero_torch, silero_onnx, funasr)
websockets>=12.0
numpy>=1.24.0
ten-vad>=1.0.0              # TEN VAD - 轻量、低延迟
webrtcvad-wheels>=2.0.0     # WebRTC VAD - 超轻量、经典
# torch>=2.0.0              # Silero VAD PyTorch 版（可选，待实现）
# torchaudio>=2.0.0         # Silero VAD PyTorch 版（可选，待实现）
onnxruntime>=1.16.0         # Silero VAD ONNX 版
# funasr                    # FunASR FSMN-VAD（可选，待实现）

# ASR (内置 Whisper)
faster-whisper>=1.0.0
